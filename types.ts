// types.ts (sirf props / types)

export interface UserProps {
  name: string;
  age: number;
}

export interface ButtonProps {
  text: string;
  onClick: () => void;
}

// 👉 rule: sirf types / interfaces